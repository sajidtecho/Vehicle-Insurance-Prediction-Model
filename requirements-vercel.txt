# Minimal dependencies for Vercel deployment
# This file contains only what's needed to run the prediction API

# Web framework
flask==2.3.0
Werkzeug==2.3.0

# Data processing
pandas==2.0.0
numpy==1.24.0

# Machine learning
scikit-learn==1.2.2
imbalanced-learn==0.10.1

# Templates
jinja2==3.1.2
MarkupSafe==2.1.2

# Configuration
PyYAML==6.0
python-dotenv==1.0.0

# Security
certifi==2023.7.22

# Path management (lightweight)
from-root==2.0.1

# Model serialization
dill==0.3.6

# Note: For Vercel deployment, heavy dependencies like matplotlib, 
# plotly, boto3, and pymongo are excluded to reduce build time and size
